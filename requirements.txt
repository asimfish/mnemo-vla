numpy>=1.24
# Optional extras (see pyproject.toml [project.optional-dependencies]):
#   faiss-cpu>=1.7          # for production-scale retrieval
#   sentence-transformers>=2.2  # for semantic embeddings
#   pytest>=7               # for running the test suite
